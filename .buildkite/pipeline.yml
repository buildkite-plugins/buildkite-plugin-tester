steps:
  - label: ":docker: :hammer:"
    plugins:
      docker-compose#v3.0.3:
        run: tests
  - wait
  - label: ":docker: :rocket:"
    plugins:
      docker-login#v2.0.1: ~
      docker-compose#v2.5.1:
        push: release
    branches: master
